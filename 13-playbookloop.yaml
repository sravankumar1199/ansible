- name: example of loops
  hosts: localhost
  tasks:
  - name: print the names
    ansible.builtin.debug:
     msg: "hello....{{item}}" ## this is loops topic in ansible ##
    loop:
    - sravan
    - mallesh
    - vittal
    - sharath
    - shashi kanth